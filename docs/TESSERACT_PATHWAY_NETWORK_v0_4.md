
# Tesseract Pathway Network v0.4

v0.4 turns TPN into a weight-bearing local mind core.

## Additions

```text
TesseractCheckpointConfig
train_tpn_checkpoint()
save_tpn_checkpoint()
load_tpn_checkpoint()
TesseractMindCore
artifacts/tpn/tpn_mind_core_v0_4.pt
artifacts/tpn/tpn_mind_core_v0_4.json
```

## Meaning

The TPN is no longer only architecture. It now has durable local weights that can be trained, saved, loaded, and run again without external calls.

## Runtime path

```text
local vector/state
→ local weighted TPN
→ selected tesseract vertex
→ sparse pathway experts
→ route logits
→ receipt
```

## Claim boundary

This checkpoint is a local synthetic route-governance seed. It is not world knowledge, AGI, production safety, or autonomous authority.

The purpose is to give the system a durable geometric mind-core substrate that can evolve through better training data and system-specific receipts.
