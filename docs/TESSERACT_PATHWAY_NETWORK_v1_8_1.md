# Tesseract Pathway Network v1.8.1

v1.8.1 is a runtime hygiene and recommendation-alignment close.

## Purpose

v1.8 proved goal-aware cycle selection. v1.8.1 closes two polish wounds:

```text
1. Remove Python runpy warning from PowerShell runners.
2. Update stale goal summary wording now that goal-aware cycle selection exists.
```

## Boundary

This is runtime hygiene only. It does not add continuous autonomy, code mutation authority, or new execution power.
