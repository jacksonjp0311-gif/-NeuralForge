# Tesseract Pathway Network v1.13-compressed

v1.13-compressed adds the Compressed Control Bundle.

## Purpose

This version compresses three safe adjacent control layers into one governed module:

```text
drift/regression sentinel
+ patch proposal receipts
+ human approval gate
```

## Boundary

The bundle observes, scores, proposes, and requests human approval. It does not apply patches, mutate code, run continuously, or claim AGI.

## Run

```powershell
.\scripts\run_tesseract_control_bundle.ps1
```
